// CountriesController removed — not required by the spec.
// The Razor Pages website loads countries directly from the database (no API needed).
// The WinForms client only looks up players by ID (GET /api/players/{id}).
